<%= form_for(current_utilisateur.active_relationships.build) do |f| %>
  <div><%= hidden_field_tag :followed_id, @utilisateur.id %></div>
  <%= f.submit "Follow", class: "btn btn-primary" %>
<% end %>